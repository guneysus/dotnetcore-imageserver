version: '3'
services:
  imageserver:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - app

  app:
    image: guneysu/dotnetcore-imageserver:0.0.1
    ports:
      - 5000:5000
